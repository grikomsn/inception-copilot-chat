---
"inception-copilot-chat": minor
---

Adds a Context Window picker control (Auto, 64K, Maximum) that caps how much conversation history each request sends, clamped to the model's registered input limit.